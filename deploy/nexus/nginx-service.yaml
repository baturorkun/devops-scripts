apiVersion: v1
kind: Service
metadata:
  labels:
    app: proxy
  name: host
spec:
  ports:
    - name: host
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: proxy
  sessionAffinity: None
  type: ClusterIP