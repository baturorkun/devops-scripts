apiVersion: v1
kind: Service
metadata:
    labels:
      app: nexus3
    name: nexus3
spec:
    ports:
      - name: docker
        port: 80
        protocol: TCP
        targetPort: 8083
    selector:
      deploymentconfig: nexus3
    sessionAffinity: None
    type: ClusterIP